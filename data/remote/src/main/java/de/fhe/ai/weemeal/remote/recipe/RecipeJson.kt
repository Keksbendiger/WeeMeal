package de.fhe.ai.weemeal.remote.recipe

/**
 * remote is currently not in use, but will implemented in a further release
 */
data class RecipeJson(
    var id: Long = 0,
    var name: String = "",
)
